services:
  - type: web
    name: mern-crud-backend
    env: node
    buildCommand: cd ../client && npm install && VITE_API_BASE_URL=/api npm run build && cd ../server && npm install
    startCommand: npm start
    rootDir: server
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://madhugullipalli_db_user:2005@cluster0.ce9t9cg.mongodb.net/bookdb?retryWrites=true&w=majority&appName=Cluster0
      - key: PORT
        value: 8800
      - key: JWT_SECRET
        value: any-random-long-string-here
